apiVersion: v1
kind: Service
metadata:
  name: {{ include "authentik.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "authentik.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http

